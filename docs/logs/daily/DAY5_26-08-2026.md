# 2026-08-26

## Work done
**ERA5-Land (meteorology)**
- Diagnosed and fixed overpass window gap: `window_hours` corrected from `[5,6,7]` to `[5,6,7,8]` after confirming ERA5-Land's temperature/dewpoint/wind bands are instantaneous parameters (point-in-time snapshots), not block averages like MERRA-2 — 4 snapshots needed to cover the full 10:30–13:30 IST window (13:30 IST falls at UTC hour 8)
- Reran `window_qc_era5_land` (45,990 → 61,320 rows) and `aggregate_era5_land_daily` (n_hours_used now 4 for all 15,330 station-days)
- Recovered accidentally deleted `scripts/datasets/era5_land/` folder via `git checkout delhi-phase1-v12 -- scripts/datasets/era5_land/`
- Committed the hour-8 fix; deleted stale tag `delhi-phase1-v11`, retagged as `delhi-phase1-v12`

**ERA5 BLH (full ERA5, meteorology)**
- Confirmed via GEE console check that `ECMWF/ERA5/HOURLY` contains a `boundary_layer_height` band (not visible in the catalog page's truncated band list); confirmed units are plain meters, no scale factor
- Sanity-checked BLH values: summer (2025-06-15, 07:00 UTC) = 1402.91m, monsoon (2025-07-15, 07:00 UTC) = 912.10m — correct seasonal direction
- Built and ran all 4 pipeline scripts:
  - Script 1: station-to-cell mapping, 42 stations → 4 unique cells (16/17/4/5 per cell)
  - Script 2: raw hourly extraction, quarterly-chunked, 4 cells × 8,760 hours = 35,040 rows
  - Script 3: window+QC (tolerance 0.05°, window_hours [5,6,7,8], QC range 0–6000m), 367,920 → 61,320 rows, BLH range 72.18–5035.4m
  - Script 4: daily aggregation, 15,330 station-days, n_hours_used=4 for all

## Key decisions
- ERA5-Land's `[5,6,7]` window undercounted by one hour because its parameters are instantaneous, unlike MERRA-2's block-averaged fields — MERRA-2 stays at `[5,6,7]` (no change needed), both ERA5-Land and ERA5 BLH use `[5,6,7,8]`
- BLH QC range set to 0–6000m — generous headroom above the two confirmed spot-check values (912m, 1403m)
- Coordinate tolerance set per grid spacing, not copied across modules: 0.01° for ERA5-Land (~11km cells), 0.05° for ERA5 BLH (~31km cells, matching MERRA-2's convention)
- BLH needs no unit/scale conversion — ECMWF reanalysis bands store direct physical values, unlike MAIAC's scaled MODIS integers
- Deleted stale tag `delhi-phase1-v11`, retagged as `delhi-phase1-v12` rather than reusing the old name, to keep tag history honest about what changed

## Blockers
- None — file attachment path in this chat repeatedly failed to deliver document content (GEE console output, CSVs); worked around with Ctrl+F + short text pastes

## Next
- ERA5 meteorology module (Temp, RH, Wind Speed, BLH) fully closed
- Resume static GEE layers thread (WorldCover buffer radius, NDVI temporal handling) — paused to prioritize BLH
- Still pending: CPCB↔AOD temporal join, MAIAC-MERRA2 gap-fill calibration order