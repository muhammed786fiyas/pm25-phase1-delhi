# 2026-08-17

## Work done
**GEE setup**
- Signed up for Google Earth Engine, created Cloud Project `internship-pm2.5`, enabled the Earth Engine API
- Installed `earthengine-api`, authenticated via `earthengine authenticate`, verified with a test script

**MAIAC AOD**
- Validated collection ID and bands in the Code Editor: `MODIS/061/MCD19A2_GRANULES` (not the deprecated `006` version), bands `Optical_Depth_055` (primary), `Optical_Depth_047`, `AOD_Uncertainty`, `AOD_QA`
- Diagnosed and fixed a `filterBounds` false-positive issue (unrelated MODIS tiles matching on swath geometry) using per-image `reduceRegion` + null-filtering
- Wrote `scripts/datasets/maiac_aod/1-extract_gee_covariates.py` — raw overpass-level, single-pixel point extraction
- Tested on 3 stations, then ran the full extraction against all 56 candidate stations (KEEP + DROP together, unfiltered) — output at `data/raw/maiac/maiac_aod_raw.csv`, 17,767 rows across 56 stations

**Station list**
- Clarified and corrected project memory: working station count is 47 (KEEP), not 44 — the <60% completeness-based drop to 44 was only a planned future QC step, never executed
- Confirmed via `rows_per_station_per_season.csv` that several stations (301, 6936, 10820, 10825, 10900) have severe seasonal gaps, likely candidates for that completeness drop
- Renamed `cpcb_stations_delhi_filtered_2025.csv` to `cpcb_stations_delhi_status.csv` (git mv, since `data/stations/` is git-tracked)

**Project logs**
- Set up `docs/logs/tasks/cpcb.md` and `docs/logs/tasks/maiac.md`, backfilled from work to date

**DVC**
- Added `maiac_extract` stage to `dvc.yaml`, matching the actual command/file used
- Narrowed CPCB stage dependencies from the whole `data/stations` folder to the specific `cpcb_stations_delhi_status.csv` file, to avoid unnecessary reruns
- Used `dvc commit --force` on `maiac_extract` to track the already-completed output without re-running the GEE extraction

## Key decisions
- Correct pipeline order confirmed: CPCB preprocessing/completeness QC → finalize station list → filter AOD to finalized stations → AOD preprocessing (scale + QA decode) → temporal alignment → MERRA-2 gap-fill → ERA5 extraction → feature assembly. CPCB QC must run before station-list finalization, not after, since the list is derived from the QC output.
- Ran the full MAIAC extraction against the unfiltered (56-station) list rather than waiting for CPCB QC, to avoid re-running the slow GEE pull later — filtering the AOD output down afterward is cheap.
- AOD extraction kept at raw overpass level (no Terra/Aqua averaging) and single-pixel (no 3x3 buffer) for the first working version — both are deliberate, deferred design choices, not oversights.
- DVC dependencies narrowed to specific files rather than whole folders where practical, to stop unrelated file changes from triggering unnecessary stage reruns.

## Discussed / raised (not decided)
- Whether to add 3x3 pixel buffer averaging around each station (vs. current single-pixel extraction) — deferred until single-pixel results are validated; would compare the two before deciding.
- Git tag naming for today's checkpoint — considered `delhi-phase1-v2` vs. a lighter tag like `maiac-raw-extraction-v1`, since CPCB QC and AOD filtering are still pending. Not yet decided.

## Blockers
- CPCB completeness QC (the <60% threshold pass) still needs to be run before the station list can be finalized and AOD data filtered down.

## Next
- Run CPCB completeness QC and finalize the station list.
- Filter `maiac_aod_raw.csv` down to the finalized station list.
- Apply AOD scale factor (×0.001) and decode `AOD_QA`.